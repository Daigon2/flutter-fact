---
name: update-repository-memory
description: Promote a verified recurring learning into the correct FACT repository artifact without bloating global instructions.
---

# Update Repository Memory

Input: `$ARGUMENTS`

Evaluate whether the learning is verified, repeated, stable and broadly useful.

Route it to exactly one primary home:

- universal fact → `CLAUDE.md`
- path-specific invariant → `.claude/rules/`
- procedure → skill
- specialist responsibility → agent
- deterministic requirement → hook/CI
- durable costly decision → ADR
- feature contract → feature docs

Avoid duplication. Show the proposed change and why this location is correct.

## References

Load only the task-relevant documents selected by `docs/ai/context-routing.md`. Do not duplicate their rules here.
